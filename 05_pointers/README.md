# Pointers

- `Phase - 01` — pointer basics and arithmetic
- `Phase 02` — function pointers and multi-level indirection
- `Phase 03` — aliasing, arrays, and `const`-correct APIs
- `Phase 04` — void pointers and double indirection
- `Phase 05` — pointer hygiene and defensive patterns
- `Phase 06` — container_of, intrusive lists, and generic containers
- `Phase 07` — expert pointer containers, ownership, and genericity
